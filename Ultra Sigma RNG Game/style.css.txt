body {
  text-align: center;
}

button {
  color: darkred;
  font-size: 50px;
  background-color: red;
  padding: 20px;
  border-radius: 100px;
  text-shadow: 3px 1px 2px black;
  width: 350px;
}

#rarist {
  font-size: 50px;
  text-shadow: 3px 1px 5px black;
}

#rolls {
  font-size: 25px;
  text-shadow: 3px 1px 5px black;
}

h1 {
  height: 50px;
  color: darkred;
  font-size: 75px;
  text-shadow: 5px 2px 5px black;
}

/* ================= FLASH ================= */
#flash {
  position: fixed;
  inset: 0;
  width: 100vw;
  height: 100vh;
  pointer-events: none;
  opacity: 0;
  background: white;
  z-index: 2147483647;
}

/* ================= SHAKE (FIXED DUPLICATE) ================= */
@keyframes shake {
  0% {
    transform: translate(0, 0) rotate(0deg);
  }
  10% {
    transform: translate(-12px, -6px) rotate(-1deg);
  }
  20% {
    transform: translate(12px, 6px) rotate(1deg);
  }
  30% {
    transform: translate(-14px, 8px) rotate(-1deg);
  }
  40% {
    transform: translate(14px, -8px) rotate(1deg);
  }
  50% {
    transform: translate(-12px, 6px) rotate(-1deg);
  }
  60% {
    transform: translate(12px, -6px) rotate(1deg);
  }
  70% {
    transform: translate(-14px, 8px) rotate(-1deg);
  }
  80% {
    transform: translate(14px, -8px) rotate(1deg);
  }
  90% {
    transform: translate(-6px, 4px) rotate(0deg);
  }
  100% {
    transform: translate(0, 0) rotate(0deg);
  }
}

.shake {
  animation: shake 0.14s infinite;
  will-change: transform;
}

/* ================= BREAKTHROUGH ================= */
@keyframes breakthroughFade {
  0% {
    opacity: 0;
    transform: scale(0.85);
    filter: blur(10px);
  }
  100% {
    opacity: 1;
    transform: scale(1);
    filter: blur(0px);
  }
}

.breakthrough {
  animation: breakthroughFade 0.7s ease-out;
}

/* ================= FLICKER ================= */
@keyframes bwFlicker {
  0% {
    background: black;
  }
  25% {
    background: white;
  }
  50% {
    background: black;
  }
  75% {
    background: white;
  }
  100% {
    background: black;
  }
}

.flicker {
  animation: bwFlicker 0.4s linear 3;
}

/* ================= COSMIC ================= */
@keyframes cosmicGlow {
  0% {
    color: #b300ff;
    text-shadow: 0 0 5px #b300ff, 0 0 10px #7a00cc;
  }
  50% {
    color: #7a00cc;
    text-shadow: 0 0 15px #b300ff, 0 0 30px #3a007a;
  }
  100% {
    color: #d24dff;
    text-shadow: 0 0 5px #d24dff, 0 0 15px #7a00cc;
  }
}

.cosmic {
  animation: cosmicGlow 1.2s ease-in-out infinite;
}

/* ================= BEST RARITY ================= */
#bestRarity {
  font-size: 28px;
  margin-top: 10px;
  text-shadow: 0 0 1px black, 2px 2px 5px black;
}

@keyframes bestUpgrade {
  0% {
    transform: scale(0.8);
    opacity: 0;
    filter: blur(6px);
  }
  50% {
    transform: scale(1.1);
    opacity: 1;
    filter: blur(0px);
  }
  100% {
    transform: scale(1);
  }
}

.upgrade {
  animation: bestUpgrade 0.5s ease-out;
}

/* ================= OVERLAYS ================= */
#gameWrapper {
  width: 100vw;
  height: 100vh;
  position: relative;
  z-index: 10;
}

h1,
#rarist,
#rolls,
#bestRarity,
button {
  position: relative;
  z-index: 20;
}

#flash {
  z-index: 50;
}
#realityOverlay {
  z-index: 60;
}
#sparkleContainer {
  z-index: 70;
}

/* ================= REALITY ================= */
#realityOverlay {
  position: fixed;
  inset: 0;
  pointer-events: none;
  opacity: 0;
  background: black;
  box-shadow: 0 0 80px white inset;
}

/* ================= SPARKLES (FIXED DUPLICATES) ================= */
#sparkleContainer {
  position: fixed;
  inset: 0;
  pointer-events: none;
  overflow: hidden;
  z-index: 70;
}

.sparkle,
.glitch-sparkle {
  position: absolute;
  top: -10px;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  pointer-events: none;
  animation: fall linear forwards;
}

.sparkle {
  background: white;
  opacity: 0.9;
  box-shadow: 0 0 10px white, 0 0 20px white;
}

.glitch-sparkle {
  opacity: 0.95;
  box-shadow: 0 0 10px currentColor, 0 0 20px currentColor;
}

/* shared fall animation */
@keyframes fall {
  from {
    transform: translateY(-10vh);
    opacity: 1;
  }
  to {
    transform: translateY(110vh);
    opacity: 0;
  }
}

/* ================= RAINBOW ================= */
.rainbow-text {
  background: linear-gradient(
    90deg,
    red,
    orange,
    yellow,
    lime,
    cyan,
    blue,
    magenta,
    red
  );
  background-size: 300%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: rainbowShift 2s linear infinite;
}

@keyframes rainbowShift {
  0% {
    background-position: 0%;
  }
  100% {
    background-position: 300%;
  }
}

/* ================= DIVINE ================= */
.divine {
  background: linear-gradient(90deg, #ffd700, #fff8b0, #ffcc00, #ffd700);
  background-size: 300%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: divineShine 2s linear infinite;
  text-shadow: 0 0 10px gold, 0 0 30px orange;
}

@keyframes divineShine {
  0% {
    background-position: 0%;
  }
  100% {
    background-position: 300%;
  }
}

/* ================= NULLBOUND (FIXED DUPLICATES REMOVED) ================= */
.nullbound-active {
  position: fixed !important;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) !important;
  z-index: 9999999;

  font-size: 4rem;
  color: #bfbfbf;
  text-shadow: 0 0 20px white, 0 0 60px cyan;
  filter: grayscale(1) contrast(1.4);
}

#bestRarity {
  display: inline-block;
  gap: 6px;
}

#illusionalBlue,
#illusionalWhite {
  position: fixed;
  top: 50%;
  left: 50%;
  width: 0px;
  height: 0px;
  transform: translate(-50%, -50%);
  pointer-events: none;
  z-index: 999999;
  opacity: 0;
}

/* light blue square */
#illusionalBlue {
  background: #7fd7ff;
  box-shadow: 0 0 40px #7fd7ff, 0 0 80px white;
}

/* white square */
#illusionalWhite {
  background: white;
  box-shadow: 0 0 50px white, 0 0 100px #7fd7ff;
}

.illusional-square {
  position: fixed;
  top: 50%;
  left: 50%;
  width: 0px;
  height: 0px;
  transform: translate(-50%, -50%);
  pointer-events: none;
  z-index: 999999;
  opacity: 0.9;
}

#illusionalText {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.2);

  font-size: 5rem;
  font-weight: bold;
  color: white;
  letter-spacing: 4px;

  opacity: 0;
  z-index: 1000000;
  pointer-events: none;
  white-space: nowrap;

  /* 💠 BLUE OUTLINE + GLOW (NO BLACK) */
  text-shadow: -2px -2px 0 rgba(127, 215, 255, 0.8),
    2px -2px 0 rgba(127, 215, 255, 0.8), -2px 2px 0 rgba(127, 215, 255, 0.8),
    2px 2px 0 rgba(127, 215, 255, 0.8), 0 0 12px rgba(127, 215, 255, 0.9),
    0 0 28px rgba(255, 255, 255, 0.6);
}

body.shake-active {
  transform: translate(var(--shake-x), var(--shake-y));
  transition: transform 0.05s linear;
}

/* ================= ILLUSIONAL (FULL SYSTEM FIX) ================= */
.illusional {
  background: linear-gradient(
    90deg,
    #5fd0ff,
    #ffffff,
    #8fe2ff,
    #eafaff,
    #ffffff,
    #5fd0ff
  );
  background-size: 260%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;

  animation: illusionalShine 1.1s linear infinite;

  /* 🔵 CLEAN DARK BLUE OUTLINE (balanced, not heavy) */
  text-shadow: -1px -1px 0 rgba(0, 55, 140, 0.65),
    1px -1px 0 rgba(0, 55, 140, 0.65), -1px 1px 0 rgba(0, 55, 140, 0.65),
    1px 1px 0 rgba(0, 55, 140, 0.65), 0 0 10px rgba(127, 215, 255, 0.8),
    0 0 22px rgba(127, 215, 255, 0.55), 0 0 34px rgba(255, 255, 255, 0.3);

  /* ✨ readability without overbrightening */
  filter: contrast(1.45) brightness(1.15) saturate(1.25);

  /* 🚀 GPU smoothing (fixes lines + shimmer artifacts) */
  transform: translateZ(0);
  backface-visibility: hidden;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;

  will-change: transform, filter, background-position;
}

/* ================= SHINE ================= */
@keyframes illusionalShine {
  0% {
    background-position: 0%;
  }
  100% {
    background-position: 260%;
  }
}

/* ================= DISTORTION LAYER ================= */
@keyframes illusionalDistort {
  0% {
    filter: hue-rotate(0deg) blur(0px);
  }
  25% {
    filter: hue-rotate(2deg) blur(0.15px);
  }
  50% {
    filter: hue-rotate(-2deg) blur(0.2px);
  }
  75% {
    filter: hue-rotate(1deg) blur(0.1px);
  }
  100% {
    filter: hue-rotate(0deg) blur(0px);
  }
}

.illusional-distort {
  animation: illusionalDistort 0.22s infinite ease-in-out;
}

/* ================= FIXED POSITION SYSTEM ================= */
#illusionalText {
  position: fixed;
  top: 50%;
  left: 50%;

  transform: translate(-50%, -50%) translate(var(--dx, 0px), var(--dy, 0px))
    scale(var(--scale, 0.2));

  font-size: 5rem;
  font-weight: bold;
  letter-spacing: 4px;

  opacity: 0;
  z-index: 1000000;
  pointer-events: none;
  white-space: nowrap;
}
